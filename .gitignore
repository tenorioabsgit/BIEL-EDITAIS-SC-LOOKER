# --- Sessao e estado do R / RStudio ---
.Rhistory
.RData
.Ruserdata
.Rproj.user/

# --- renv: nao versionar a library, apenas o lockfile ---
renv/library/
renv/local/
renv/cellar/
renv/lock/
renv/staging/

# --- Logs ---
*.log

# --- Credenciais: NUNCA versionar chaves de acesso ---
credentials/
*-key.json
*service-account*.json
client_secret*.json
BigQuery.json
dou-sc.json
gcp-key.json

# --- Config local (segredos) ---
config/config.local.yml

# --- Dados gerados pela execucao ---
backup_bases/
arquivos_downloaded/
*.rds
base_consolidada*.csv
base_final*.csv

# --- Sistema operacional ---
.DS_Store
Thumbs.db
*.desktop
